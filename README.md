# CW-04: React Fundamentals

Files included:

- `src/components/HelloWorld.jsx`  
- `src/components/Counter.jsx`  
- `src/components/List.jsx`  
- `src/components/FilteredList.jsx`  
- `src/App.js`  

Run locally:

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
```

Deployment: build the site and publish the `dist` output to GitHub Pages or your hosting.

Reflection answers are in `REFLECTIONS.md`.
